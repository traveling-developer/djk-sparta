---
interface Props {
    name: string;
    league: string;
    link: string;
    rank: string;
    points: string;
}

const { name, league, link, rank, points } = Astro.props;
---

<div>
    <div
        class="card w-96 text-white bg-gradient-to-b from-[#8b0000] via-spartaRed to-[#ff6666]"
    >
        <div class="card-body">
            <h2 class="card-title justify-center">{name}</h2>
            <p>
                {league}<br />
                Tabellenrang {rank}<br />
                Punkte {points}
            </p>
            <div class="card-actions justify-center">
                <a href={link} target="_blank" class="btn btn-outline mt-4"
                    >Click-TT</a
                >
            </div>
        </div>
    </div>
</div>
