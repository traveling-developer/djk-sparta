---
import ContactCard from "../ContactCard.astro";

interface Props {
  isOnContactsPage?: boolean;
}

const { isOnContactsPage = false } = Astro.props;
---

<ContactCard
  title={isOnContactsPage
    ? "Sabine Fuchs (Gesundheitssport)"
    : "Sabine Fuchs (Abteilungsleiterin)"}
  mobile="0157 746 079 63"
/>
